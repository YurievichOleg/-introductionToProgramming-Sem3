Console.WriteLine("Введите число:  ");
string chislo = Console.ReadLine();
int a = Convert.ToInt32(chislo);
void chetnoe(int x)
{
    if(x % 2 ==0)
    {
    Console.WriteLine($"{x} - чётное");
    }
    else Console.WriteLine($"{x} - нечётное");
    
        
}
chetnoe(a);
